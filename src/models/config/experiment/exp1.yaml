dirs:
  processed_path: '../data/processed'
  

hyperparameters:
  pretrained_model: 'google/vit-base-patch16-224-in21k'
  batch_size:
  x_dim:
  hidden_dim:
  latent_dim:
  lr: 0.001
  epochs: 3
  seed: 42
  gpu: False